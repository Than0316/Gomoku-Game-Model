class Config:
    size = 15
    points_limit = 20 # Limit for number of moves to return in heuristic search
    inline_count = 4  # Used for "in-line" optimization (not fully implemented in basic version but kept for compatibility)
    inline_distance = 5